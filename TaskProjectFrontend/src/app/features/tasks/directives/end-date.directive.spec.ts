import { EndDateDirective } from './end-date.directive';

describe('EndDateDirective', () => {
  it('should create an instance', () => {
    const directive = new EndDateDirective();
    expect(directive).toBeTruthy();
  });
});
