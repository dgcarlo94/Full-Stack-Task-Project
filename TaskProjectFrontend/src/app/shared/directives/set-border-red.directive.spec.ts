import { SetBorderRedDirective } from './set-border-red.directive';

describe('SetBorderRedDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBorderRedDirective();
    expect(directive).toBeTruthy();
  });
});
